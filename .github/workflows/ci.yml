name: MLflow Project CI & Docker

on:
  push:
    paths:
      - 'MLproject/**'
      - '.github/workflows/ci.yml'
  workflow_dispatch:

jobs:
  train-and-docker:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: mariouskono/workflow-ci
      DAGSHUB_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12' # Mengubah dari '3.10' ke '3.12' untuk konsistensi

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Menginstal versi yang tepat seperti yang ditemukan di MLproject/mlruns/0/models/.../artifacts/requirements.txt
          pip install "mlflow==3.1.0"
          pip install "cloudpickle==3.1.1"
          pip install "numpy==2.0.2"
          pip install "pandas==2.2.2"
          pip install "psutil==7.0.0"
          pip install "scikit-learn==1.6.1"
          pip install "scipy==1.15.3"
          pip install "joblib==1.3.2" # Versi Joblib dari MLproject/conda.yaml
          pip install "python-dotenv==1.0.0" # Versi python-dotenv dari MLproject/conda.yaml
          pip install dagshub
          pip install matplotlib # Tetap sertakan jika digunakan untuk plotting/visualisasi
          pip install seaborn # Tetap sertakan jika digunakan untuk plotting/visualisasi

      - name: Verify installation
        run: |
          python -c "import numpy, pandas, sklearn, matplotlib, seaborn, mlflow, joblib; print('✅ All packages imported successfully')"

      - name: Run MLflow Project
        run: |
          cd MLproject
          mlflow run . --env-manager=local

      - name: Read Run ID
        id: read_run_id # Menambahkan ID ke langkah untuk referensi outputnya
        run: |
          cd MLproject
          # Menggunakan GITHUB_OUTPUT untuk meneruskan ID run ke langkah-langkah berikutnya
          RUN_ID=$(cat mlflow_run_id.txt)
          echo "run_id_output=$RUN_ID" >> $GITHUB_OUTPUT

      - name: Build Docker Image from MLflow Model
        run: |
          cd MLproject
          # Menggunakan output dari langkah sebelumnya untuk mendapatkan RUN_ID
          mlflow models build-docker -m "mlruns/0/${{ steps.read_run_id.outputs.run_id_output }}/artifacts/tfidf_model" -n ${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker Image
        run: |
          docker push ${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: model-artifacts
          path: MLproject/**/*.joblib
